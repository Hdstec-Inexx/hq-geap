# Banco de dados (PostgreSQL)
DATABASE_URL=postgres://hq_geap:hq_geap@127.0.0.1:5432/hq_geap
TEST_DATABASE_URL=postgres://hq_geap:hq_geap@127.0.0.1:5432/hq_geap_test

# Aplicações locais
HOST=127.0.0.1
PORT=3000
CORS_ORIGIN=http://localhost:5173
VITE_API_URL=http://localhost:3000
JWT_SECRET=
JWT_EXPIRES_IN_SECONDS=28800

# As chaves da ElevenLabs e do OpenRouter e a credencial de escrita no
# PostgreSQL pertencem ao cofre de Credentials do n8n, não ao ambiente do HQ.
